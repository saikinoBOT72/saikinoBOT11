# カスタム絵文字の作り方

ゲームの見た目に使う絵文字は、**⚙️ 管理 → 😀 絵文字の設定** からサーバーのカスタム絵文字に差し替えられます。
差し替えなければ、いまの Unicode 絵文字（Discord では Twemoji として表示）のままです。

## 先に知っておくこと

- **無料サーバーはカスタム絵文字50個まで**（ブースト1で100個、2で150個）。下の「最優先」だけなら11個で収まります。
- **画像は 128×128 px の PNG（背景透過）** で作ってください。Discord 側で縮小されます。1個あたり **256KB まで**。
- **表示は小さい**（本文中で約22px、大きな見出しで約32px）。細い線や小さい文字は潰れます。**太い線・大きい要素・強いコントラスト**で作ってください。
- 名前は英数字とアンダースコアのみ。**下の表の名前をそのまま使う**と、あとで迷いません。
- Twemoji の元画像は <https://github.com/jdecked/twemoji/tree/main/assets/svg> にコードポイント名の SVG があります（例 `1f3b0.svg` = 🎰）。これを下敷きにすると、既存の絵文字と絵柄が揃います。ライセンスは **CC-BY 4.0**（利用条件は上記リポジトリを確認してください）。

## 最優先で作るもの（11個）

Unicode では**絵文字にならず、細い文字として表示されてしまう**ものです。ここを作るだけで見違えます。

### サイコロの目（6個）

| 名前 | いまの表示 | 作るもの |
| --- | --- | --- |
| `dice1` | ⚀ | 白いサイコロ、赤い1の目（ピンゾロが映えるので1だけ赤がおすすめ） |
| `dice2` | ⚁ | 白いサイコロ、黒い2の目 |
| `dice3` | ⚂ | 白いサイコロ、黒い3の目 |
| `dice4` | ⚃ | 白いサイコロ、黒い4の目 |
| `dice5` | ⚄ | 白いサイコロ、黒い5の目 |
| `dice6` | ⚅ | 白いサイコロ、黒い6の目 |

作り方のコツ:

- **6個で絵柄を完全に統一**する（角の丸み・影・傾き・目の大きさ）。横に3つ並ぶので、バラつくと目立ちます。
- 正面向きの平面（アイソメトリックな立体にしない）。小さいと立体は潰れます。
- 目（ピップ）は**大きめ・少なめの余白**で。128px なら直径 20〜24px くらい。
- 傾けるなら**全部同じ角度**に。

### トランプのスート（4個）

| 名前 | いまの表示 | 作るもの |
| --- | --- | --- |
| `spade` | ♠️ | 黒いスペード（Twemoji `2660`） |
| `heart` | ♥️ | 赤いハート（Twemoji `2665`） |
| `diamond` | ♦️ | 赤いダイヤ（Twemoji `2666`） |
| `club` | ♣️ | 黒いクラブ（Twemoji `2663`） |

ハイ&ローでは `♠️A` のように **スート＋数字** で表示します。スートだけ作れば数字は文字のままで揃うので、52枚は不要です。

> 52枚ぜんぶ作りたい場合は `card_ace_spade` のような名前で 52個必要になり、無料枠を超えます。ブーストしていなければスート4個をおすすめします。

### カードの裏（1個）

| 名前 | いまの表示 | 作るもの |
| --- | --- | --- |
| `card_back` | 🂠 | カードの裏面。めくる演出の「？」の代わりに出ます |

## 余裕があれば作るもの（ゲームのアイコン・11個）

いまも Twemoji で表示されているので急ぎではありませんが、**サイコロやカードを自作の絵柄にすると、そこだけ浮く**ことがあります。揃えたいならこちらも。

| 名前 | いまの表示 | Twemoji のファイル名 |
| --- | --- | --- |
| `slot` | 🎰 | `1f3b0` |
| `coinflip` | 🪙 | `1fa99` |
| `highlow` | 🃏 | `1f0cf` |
| `rps` | ✊ | `270a` |
| `chinchiro` | 🎲 | `1f3b2` |
| `poll` | 🗳️ | `1f5f3` |
| `roulette` | 🔫 | `1f52b` |
| `doors` | 🚪 | `1f6aa` |
| `charge` | ⚡ | `26a1` |
| `mines` | 💣 | `1f4a3` |
| `lottery` | 🎫 | `1f3ab` |

`dice_roll`（🎲）は、サイコロを振っている最中の演出に使います。**GIF にすると回転しているように見えます**（アニメーション絵文字はブースト不要ですが、枠は通常の絵文字と別枠です）。

## 登録のしかた（スマホでできます）

1. Discord のサーバー設定 → **絵文字** → **絵文字をアップロード** で、上の名前をつけて登録
2. Bot で **⚙️ 管理 → 😀 絵文字の設定** を開く
3. リストから差し替えたいものを選ぶ
4. 入力欄に絵文字を貼る。**メッセージ欄に `\:dice1:` と打って送信**すると `<:dice1:123456789012345678>` という文字列が出るので、それをコピーして貼り付け
5. 空欄で保存すると既定に戻ります。**すべて既定に戻す** ボタンで全部戻せます

> 絵文字を貼るとき、そのままだと絵文字として表示されて中身が分かりません。**バックスラッシュ `\` を頭に付けて送信**するのがコツです。
